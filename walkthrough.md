# Walkthrough (Archived Notes)

This file previously contained exploratory notes and external links gathered during early SNOS investigation.

It is no longer the source of truth for repository documentation.

Use these maintained docs instead:

- `README.md` for usage, setup, and high-level context.
- `docs/ARCHITECTURE.md` for crate/module responsibilities.
- `docs/CODE_FLOW.md` for execution pipeline and RPC/concurrency flow.
- `CONTRIBUTING.md` for contributor workflow and validation steps.
- `CLAUDE.md` for agent-oriented navigation/runbook guidance.
